# DESIGN.md

## 이게 뭔가요

박유진의 개인 소개 페이지입니다. 스크롤 한 번으로 끝나는 단일 페이지로,
방문자가 "이 사람이 누구고, 어디서 뭘 하고 있고, 어디서 더 볼 수 있는지"를 파악하게 하는 것이 전부입니다.
글·활동·링크는 모두 데이터 파일 한 곳에서 읽어오고, 화면에는 고정된 글자가 없습니다.

## 화면에 있는 것들

위에서 아래로 다섯 덩어리입니다. 라우트는 하나뿐이고 페이지 이동은 없습니다.

| 순서 | 영역 | 들어있는 것 |
|---|---|---|
| 1 | **Hero** | 크림슨 액센트 바(짧은 가로선) → 이름 `박유진` (가장 큰 제목) → 한 줄 소개 `컴퓨터학과 25학번 · 스마트보안학부 이중전공` |
| 2 | **About** | 섹션 라벨 + 소개 문단 하나 |
| 3 | **Activities** | 섹션 라벨 + 활동 목록 5개 (최신순) |
| 4 | **Interests** | 섹션 라벨 + 관심사 태그 4개 |
| 5 | **Links** | 섹션 라벨 + 외부 링크 목록 |

### 각 영역 안쪽

- **섹션 라벨** — `About` / `Activities` / `Interests` / `Links`.
  작은 대문자에 자간을 넓게 벌린 밝은 크림슨 글씨. 제목이라기보다 구분 표지에 가깝습니다.
- **활동 목록** — 항목마다 왼쪽에 딥 크림슨 세로선이 붙습니다.
  한 항목은 세 줄까지 세로로 쌓입니다: 조직 이름(굵게) → 역할(크림슨, 작게) → 부연 설명(회색, 작게, 있을 때만).
  가로로 붙이지 않은 이유는 `정보대학 제10대 학생회 'PULSE'`처럼 이름이 길어서 좁은 화면에서 줄바꿈이 지저분해지기 때문입니다.
  - KUICS — 운영진 (고려대학교 정보대학 정보보안 동아리)
  - 컴퓨터학과 제2대 학생회 '영원' — 교육복지국장
  - 정보대학 제10대 학생회 'PULSE' — 재정사무국원
  - 컴퓨터학과 제1대 학생회 '미르' — 문화기획국원
  - 정보대학 제9대 학생회 '이음' — 재정사무국원
- **관심사 태그** — 크림슨 테두리의 둥근 알약 모양. 웹 해킹 / 리버싱 / CTF / 네트워크.
  가로로 나열되다가 화면이 좁으면 다음 줄로 넘어갑니다.
- **링크** — GitHub 하나. 크림슨 밑줄이 그어져 있고, 마우스를 올리면 글자가 밝은 크림슨으로 바뀝니다.
  새 탭에서 열립니다.

### 버튼은 없습니다

누를 수 있는 것은 Links의 외부 링크뿐입니다.
폼, 메뉴, 탭, 다크/라이트 전환 스위치 같은 조작 요소는 두지 않았습니다.

### 비어 있으면 사라집니다

About·Activities·Interests·Links는 데이터가 비면 섹션 제목까지 통째로 렌더되지 않습니다.
빈 목록 자리에 빈 제목만 남는 상황을 막기 위한 것으로, 실제로 관심사를 채우기 전에는 Interests가 없는 화면이었습니다.

### 여백과 폭

- 본문은 최대 672px까지만 넓어지고 가운데 정렬됩니다. 그 이상 넓은 화면에서는 양옆 여백만 늘어납니다.
- 좌우 여백 24px(모바일) → 32px(넓은 화면), 위아래 64px → 96px.
- 섹션 사이 간격 56px. 구분선은 쓰지 않고 여백만으로 나눕니다.
- 모바일을 먼저 맞추고 넓은 화면을 나중에 맞췄습니다. 기준 폭은 375px입니다.

## 색

검정 바탕에 크림슨 액센트, 한 가지 룩으로 고정입니다. 라이트 모드는 없습니다.

| 쓰임 | 색 | 값 |
|---|---|---|
| 배경 | 검정 | `#000000` |
| 기본 글자 | 아주 밝은 회색 | `#f4f4f5` |
| 이름(가장 큰 제목) | 흰색 | `#ffffff` |
| 한 줄 소개 | 중간 회색 | `#a1a1aa` |
| 소개 문단 | 밝은 회색 | `#d4d4d8` |
| 부연 설명 | 어두운 회색 | `#71717a` |
| **딥 크림슨** — 면·테두리·밑줄 | 짙은 자주빛 빨강 | `#862633` |
| **밝은 크림슨** — 검정 위 글자·링크 | 밝은 빨강 | `#d93b54` |

크림슨을 두 단계로 나눈 이유가 있습니다.
고려대 상징색에 가까운 `#862633`은 검정 배경 위에서 **글자로 쓰면 대비가 부족해 읽기 어렵습니다.**
그래서 액센트 바·세로선·태그 테두리·밑줄처럼 **면과 선에만** 쓰고,
섹션 라벨이나 역할 표기처럼 **읽어야 하는 글자에는 밝은 쪽**을 씁니다.

관심사 태그의 테두리는 딥 크림슨을 반투명하게 깔아 존재감을 낮췄습니다.

## 글꼴

- **Geist** — 라틴 문자(영문·숫자·섹션 라벨)에 쓰입니다.
- **한글은 시스템 기본 글꼴** — Geist에 한글이 없어서, 보는 사람의 운영체제 글꼴로 자연스럽게 넘어가도록 폴백을 걸어뒀습니다. 페이지 글자 대부분이 한글이라 실제로 보이는 건 대체로 이쪽입니다.
- 별도의 한글 웹폰트는 넣지 않았습니다. 로딩 비용 대비 이득이 크지 않다고 봤습니다.

### 크기

| 쓰임 | 크기 |
|---|---|
| 이름 | 36px → 48px (넓은 화면) |
| 소개 문단 · 조직 이름 | 16px |
| 역할 · 부연 설명 · 관심사 태그 | 14px |
| 섹션 라벨 | 12px, 대문자, 자간 넓게 |

굵기는 두 가지만 씁니다. 이름과 조직 이름은 굵게, 나머지는 보통입니다.

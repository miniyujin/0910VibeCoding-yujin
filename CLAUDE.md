# CLAUDE.md

## 프로젝트 개요

개인 소개 페이지. **단일 페이지(one-page)** 로 소개 글, 관심사, 링크만 보여준다.
방문자가 스크롤 한 번으로 "이 사람이 누구고, 뭘 좋아하고, 어디서 더 볼 수 있는지"를 파악하는 것이 목표.

## 기술 스택

- **Next.js** (App Router)
- **TypeScript**
- **Tailwind CSS**

## 데이터

- 모든 콘텐츠는 **로컬 JSON 파일**에서 읽는다. (`data/profile.json`)
- DB는 **나중에** 붙일 예정이므로, 지금은 JSON을 직접 import 해서 사용한다.
- 나중의 DB 전환을 쉽게 하려고, JSON 접근은 한 군데(예: `lib/profile.ts`)로 모으고
  컴포넌트는 그 함수만 호출한다. 컴포넌트가 JSON 파일을 직접 import 하지 않는다.
- 데이터 형태는 TypeScript 타입으로 정의해 둔다 (`types/profile.ts` 또는 `lib/profile.ts` 안에).

예상 데이터 형태:

```json
{
  "name": "...",
  "tagline": "...",
  "about": "...",
  "interests": ["...", "..."],
  "links": [{ "label": "GitHub", "url": "https://..." }]
}
```

## 페이지 구성

`app/page.tsx` 하나. 섹션은 대략:

1. **Hero** — 이름 / 한 줄 소개
2. **About** — 소개 글
3. **Interests** — 관심사 목록
4. **Links** — 외부 링크 (GitHub, 블로그 등)

라우트를 추가하지 않는다. 새 페이지가 필요해 보이면 먼저 물어볼 것.

## 지금 하지 않을 것

아래는 **명시적으로 범위 밖**이다. 요청받기 전에는 건드리지 않는다.

- ❌ **Vercel 배포** — 배포 설정, `vercel.json`, CI/CD 파이프라인 없음
- ❌ **Supabase 연동** — 클라이언트 설치/설정/환경변수 없음
- ❌ 데이터베이스, 인증, API 라우트
- ❌ CMS, 블로그, 어드민, 다국어(i18n)
- ❌ 방문자 분석/추적 스크립트

관련 작업이 필요해 보이면 코드를 추가하지 말고 먼저 제안만 할 것.

## 작업 규칙

- 스타일링은 Tailwind 유틸리티 클래스로. 별도 CSS 파일이나 CSS-in-JS 라이브러리를 추가하지 않는다.
- UI 라이브러리(shadcn/ui, MUI 등)는 물어보고 추가한다. 기본은 순수 Tailwind.
- 텍스트, 링크 같은 콘텐츠는 컴포넌트에 하드코딩하지 말고 JSON에 넣는다.
- 모바일 우선. 좁은 화면에서 먼저 확인하고 넓은 화면을 맞춘다.
- 의존성 추가는 최소로. 추가 전에 이유를 설명할 것.
